/**
 * dsh-archive-manager — browser half (TypeScript source).
 *
 * This is the TypeScript source for lib/client.js. The compiled JavaScript
 * (lib/client.js) is the actual entry point dsh loads at runtime — it must
 * self-register via window.__ModuleLoader__.load({ id, factory }).
 *
 * See lib/client.js for the full implementation: settings card injection,
 * archive listing, and deletion UI.
 */

export function apply(_ctx: any): void {
  void _ctx
}
